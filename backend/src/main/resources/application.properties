# ================================================================
#  Cartvix — application.properties
# ================================================================

server.port=8080

# ── PostgreSQL (Supabase) ────────────────────────────────────────
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.kvnfatiabfphkcjatned
spring.datasource.password=@#$Akshat&03$#@
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP tuning for Supabase pooler
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ── JWT ──────────────────────────────────────────────────────────
app.jwt.secret=cartvix_super_secret_jwt_key_2026_change_in_production_make_it_very_long
app.jwt.expiration=86400000

# ── Google Client ID (used ONLY for token verification in GoogleAuthService)
# We do NOT use Spring's OAuth2 redirect flow — frontend sends ID token directly.
# Keeping only client-id here; removing client-secret/scope to avoid
# Spring auto-configuring a redirect URI that causes Error 400.
spring.security.oauth2.client.registration.google.client-id=583049937802-9sk9huoh4hsieig3jr0vj3nih23d5icm.apps.googleusercontent.com

# ── OTP via Google Apps Script ───────────────────────────────────
app.otp.apps-script-url=https://script.google.com/macros/s/AKfycbyyX_eyL4qKi5z3V6D7k-pWW-isI2gG80Xi5SMZECmFkwY_FbELwcB5sUeI8vaZhDwm/exec
app.otp.expiry-minutes=5
app.otp.max-attempts=3

# ── CORS ─────────────────────────────────────────────────────────
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://cartvix.netlify.app

# ── File Upload ──────────────────────────────────────────────────
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB